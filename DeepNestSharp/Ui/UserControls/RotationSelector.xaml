<UserControl x:Class="DeepNestSharp.Ui.UserControls.RotationSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="70" d:DesignWidth="340">
  <UserControl.Resources>
    <!-- Button-like toggle that highlights clearly when selected (Radan-style). -->
    <Style x:Key="RotationOptionStyle" TargetType="ToggleButton">
      <Setter Property="Margin" Value="1.5,0" />
      <Setter Property="Padding" Value="2,3" />
      <Setter Property="Width" Value="46" />
      <Setter Property="Background" Value="#FFFFFF" />
      <Setter Property="BorderBrush" Value="#C8C8C8" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ToggleButton">
            <Border x:Name="bd" CornerRadius="0" BorderThickness="1.5"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="BorderBrush" Value="#666699" />
                <Setter TargetName="bd" Property="Background" Value="#D6D6E8" />
              </Trigger>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="bd" Property="BorderBrush" Value="#000080" />
                <Setter TargetName="bd" Property="Background" Value="#D6D6E8" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </UserControl.Resources>
  <!-- Viewbox (DownOnly) guarantees the row always shrinks to fit the available width and never
       overflows, whatever the window/panel size — natural size when there's room, smaller when not. -->
  <Viewbox Stretch="Uniform" StretchDirection="DownOnly" HorizontalAlignment="Left" VerticalAlignment="Top">
    <StackPanel x:Name="optionsPanel" Orientation="Horizontal" />
  </Viewbox>
</UserControl>
